syntax = "proto3";

package go.micro.api;

service Health {
    // Health 监控检查
    rpc Health(go.micro.api.Request) returns (go.micro.api.Response) {}
}

message Request {
}

message Response {
    bool valid = 1;
}